{
  "name": "engine262-website",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.11.0",
  "scripts": {
    "dev": "pnpm run --parallel --filter '@engine262/*' dev",
    "check": "pnpm run --parallel --filter '@engine262/*' check",
    "build": "pnpm run --parallel --filter '@engine262/*' build",
    "serve": "node scripts/serve.mts"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "next": "16.2.10",
    "prettier": "^3.9.5",
    "typescript": "^5.9.0"
  }
}
